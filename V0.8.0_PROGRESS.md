# v0.8.0 Build Progress — Add Graphify next to QMD

Tracks the build of dev_env_builder v0.8.0: adding Graphify (code knowledge
graph, `Graphify-Labs/graphify` / PyPI `graphifyy`) **alongside** QMD, not
replacing it. Updated after each step passes its tests. See
[QMD.md](QMD.md) for the existing tool this is complementing.

Plan (per user instruction): (1) build the test suite first, (2) implement
each asset one step at a time, testing after each step before moving to the
next and updating that step's docs once it's green, (3) update overall docs
and commit the full v0.8.0.

## Status: 🟢 all steps done — ready to commit (step 7)

Rancher Desktop crash from earlier is resolved. Base image + deploy kit with
Graphify built, deployed, and verified end-to-end (36 passed, 0 failed).
Two real bugs found and fixed during verification (see Step B below) — this
is exactly why the test-first approach was worth it. All docs updated and
version-bumped to v0.8.0. **Nothing pushed to DockerHub or GitHub yet**
except the checkpoint commit `35cd329` (test suite + this doc, from before
Steps A–C).

## Steps

- [x] **0. Survey existing build/deploy files** — read `build_and_push.ps1`,
      `Dockerfile.base_rust_dev`, `deploy-dev.ps1`, `docker-compose-dev.yml`,
      `Dockerfile.rust-dev` in full to understand current v0.7.2 wiring before
      changing anything.
- [x] **1. Test suite first** — extended `deploy_dev_env/test-deployment.sh`
      (header bumped to v0.8.0):
      - Added `uv` binary/version check + merged the old bare `Python3` check
        into a `Python3 >= 3.10` version-floor check (Graphify's requirement).
      - Added a full "Graphify (Code Knowledge Graph)" section: `graphify` /
        `graphify-mcp` binaries, and per discovered git repo —
        `graphify-out/graph.json` non-empty, `.claude/skills/graphify/SKILL.md`
        present, `.claude/settings.json` PreToolUse hook mentions graphify,
        `.git/hooks/post-commit` mentions graphify, `.gitignore` has
        `graphify-out/cost.json`, plus a `graphify-status` bashrc helper check.
      - `bash -n` syntax-validated clean.
      - **Not yet run against a live container** (Docker died before the
        baseline run completed) — do this first once Docker is back: expect
        the new Graphify checks to fail red (nothing installed yet) and
        everything else to still pass, as the "red" baseline.
- [x] **2. Step A — base image**: `build_base_dev_image/Dockerfile.base_rust_dev`
      — added `uv` (astral installer, same pattern as the existing Bun
      block), then `uv tool install "graphifyy[mcp]"` as `rustdev` (dropped
      the `leiden` extra to keep the build fast/reliable — pure prebuilt
      wheels, ~12s install, no compiler step; can revisit if clustering
      quality matters later). Ubuntu 22.04's stock Python 3.10 satisfies
      Graphify's `>=3.10` floor — no separate Python install needed. Added
      `~/.local/bin` to `PATH` (both `ENV PATH` for `docker exec`/build-time
      and a `.bashrc` export for interactive SSH shells, matching the Bun
      precedent). Added `graphify-status` bashrc helper + `graphify-reindex`
      alias (→ `~/init_graphify.sh`), mirroring `qmd-status`/`qmd-reindex`.
      Header bumped to 0.8.0. Built locally
      (`tsouche/base_rust_dev:latest` + `:v0.8.0`, **no DockerHub push**) —
      clean build, `uv 0.12.1`, `graphify 0.9.32`, `graphify-mcp` all
      installed. Smoke-tested in isolation with a throwaway
      `docker run --rm -u rustdev tsouche/base_rust_dev:latest bash -ic
      "..."`: `uv`/`graphify`/`graphify-mcp` all resolve via `ENV PATH`, and
      `graphify-status`/`graphify-reindex` work correctly under an
      interactive shell (bashrc functions need `-i`, same as pre-existing
      `qmd-status` — verified side-by-side, not a regression).
- [x] **2a. Step A docs** — added a "Version 0.8.0" entry to
      `build_base_dev_image/CHANGELOG.md` and a Graphify bullet to the
      tooling list in `build_base_dev_image/README.md`.
- [x] **3. Step B — deploy kit**: created `deploy_dev_env/init_graphify.sh`
      (mirrors `init_qmd.sh`'s repo-discovery loop). Per repo: `graphify
      extract . --code-only [--cargo]`, `graphify cluster-only .` (generates
      GRAPH_REPORT.md + graph.html — runs fully offline, no API key needed,
      just falls back to generic "Community N" labels instead of descriptive
      ones), `graphify claude install --project` (not `--strict`), `graphify
      hook install`, append `graphify-out/cost.json` to `.gitignore`
      (graph.json/GRAPH_REPORT.md/graph.html are meant to be committed —
      deliberate, opposite of QMD's gitignored SQLite). Also self-heals the
      `graphify-status`/`graphify-reindex` bashrc block on every run (see bug
      below — same root cause). Wired into `deploy-dev.ps1` (copy + run,
      right after "Initialize QMD", before "Initialize gstack"). Bumped
      `Dockerfile.rust-dev` header/bullet list + added its own `COPY
      init_graphify.sh` block. Updated `docker-compose-dev.yml` header
      comment — confirmed **no new volume needed**.
- [x] **3a. Step B verify** — rebuilt `dev-container` against the new base
      and did `up -d --force-recreate` (non-destructive: preserves the named
      home volume and the /workspace bind mount, avoids re-running the full
      interactive `cleanup.ps1`+`deploy-dev.ps1` for this iteration). Created
      a throwaway local git repo (`/workspace/test_repo`, minimal Rust
      project — GitHub auth isn't set up for outbound clone inside a fresh
      container, so this avoided that dependency entirely) to exercise the
      real code paths. Ran `init_graphify.sh` end-to-end successfully: graph
      extracted (9 nodes/9 edges/4 communities), GRAPH_REPORT.md + graph.html
      generated offline, `.gitignore` updated, Claude Code project
      integration installed (`.claude/skills/graphify/SKILL.md`,
      `.claude/CLAUDE.md`, root `CLAUDE.md` graphify section,
      `.claude/settings.json` PreToolUse hook on Bash/Grep/Read/Glob), git
      `post-commit` + `post-checkout` hooks + merge driver installed.
      Re-ran it a second time to confirm idempotency (correctly reported
      "already installed" for hooks). Ran the extended `test-deployment.sh`:
      **36 passed, 0 failed, 3 warnings** — all 10 Graphify checks green; the
      3 warnings are a **pre-existing, unrelated QMD bug** (native
      `better-sqlite3` binding blocked by npm's install-script gating —
      present since before any Graphify work started this session, visible
      in the very first base-image build log; out of scope for v0.8.0, flag
      separately). Cleaned up the throwaway test repo afterward.
- [x] **3b. Step B docs** — `deploy_dev_env/CHANGELOG.md` new v0.8.0 entry
      (see below, includes the two bugs found and fixed during verification).

### 🐛 Bugs found and fixed during Step B verification

1. **Home-volume shadowing**: `uv`/`graphify` were first installed under
   `/home/${USERNAME}/.local`. `/home/rustdev` is a persistent named Docker
   volume on deployment — Docker only seeds a volume's content from the
   image the *first* time that volume is created, so anything added under
   `/home/${USERNAME}` in a *later* image build is invisible on every
   subsequent redeploy against an already-existing volume (proven: a fresh
   `docker run` smoke test worked fine, but `docker exec` against the real
   deployed `dev-container` reported `graphify: command not found`). Fixed
   by installing `uv` and `graphify` into `/usr/local` instead (via
   `UV_INSTALL_DIR`, `UV_TOOL_DIR`, `UV_TOOL_BIN_DIR`), matching how Bun is
   already installed for the exact same reason. Same root cause meant the
   baked-in `.bashrc` helpers (`graphify-status`/`graphify-reindex`) would
   have the identical problem on any existing deployment, so
   `init_graphify.sh` now also rewrites that managed block on every run
   (idempotent, marker-delimited) — self-healing regardless of when the
   volume was first created, which is strictly more robust than relying on
   image-bake-time propagation alone.
2. **Missing `tomli` dependency**: `graphify extract --cargo` failed with
   `error: --cargo on Python 3.10 needs tomli` (Python 3.11+ has this
   built in as `tomllib`; Ubuntu 22.04's stock Python is 3.10). Fixed by
   adding `--with tomli` to the `uv tool install` command.

- [x] **4. Step C — CLAUDE.md.template**: added a Graphify section + a
      tool-routing rule at the top of the file (QMD for content/semantic
      questions, Graphify for structural/call-graph questions,
      GRAPH_REPORT.md for architecture orientation) so the two tools' hook
      nudges don't conflict. Verified by copying the template into the
      container's persistent `~/.claude/CLAUDE.md` and re-running the test
      suite: 104 lines (up from 67), 18 graphify references, 4 gstack
      references unchanged, 0 failed.
- [x] **5. New `GRAPHIFY.md`** at repo root, parallel to `QMD.md` in
      structure (where it comes from, per-repo model table, setup, daily
      usage, Claude Code integration).
- [x] **6. Overall docs** — updated root `README.md` (deliverables, arch
      diagram, key features, persistence model, repo structure, docs links,
      version footer), root `CHANGELOG.md`, `deploy_dev_env/README.md` (new
      §3.6 Graphify section mirroring §3.5 QMD, version footer),
      `deploy_dev_env/CHANGELOG.md`, `build_base_dev_image/CHANGELOG.md` +
      `README.md`, and version-header comments in `.env`,
      `docker-compose-dev.yml`, `deploy-dev.ps1`, `Dockerfile.rust-dev`,
      `build_and_push.ps1`, `test-deployment.sh` — all bumped to v0.8.0.
- [ ] **7. Commit + tag v0.8.0** — ask before pushing to GitHub; do **not**
      run `build_and_push.ps1` (DockerHub publish) unless separately asked.

## Locked-in design decisions (do not re-derive)

- **Additive, not a replacement** — QMD is untouched; Graphify is new and
  parallel. User confirmed explicitly.
- **No new Docker volume** — code-only extraction is pure tree-sitter +
  networkx, zero LLM cost, no GGUF models. Simpler than QMD's ~2GB model
  volume.
- **Path-based scoping, not flag-based** — Graphify operates on
  `./graphify-out/` relative to cwd, no `--index {name}` flag like QMD, so no
  per-project `graphify-{name}` aliases are needed — just `cd` into the repo.
- **Commit `graphify-out/graph.json` + `GRAPH_REPORT.md`** to each project
  repo (gitignore only `cost.json`) — upstream's own recommended workflow,
  deliberately the opposite of QMD's fully-gitignored SQLite index.
- **Install**: `uv` (astral installer) + `uv tool install "graphifyy[mcp,leiden]"`.
  No separate Python install (Ubuntu 22.04 ships 3.10, meets the `>=3.10` floor).
- **Per-repo Claude Code integration via `graphify claude install --project`**
  (writes the project's own CLAUDE.md + `.claude/skills/graphify/SKILL.md` +
  a `PreToolUse` hook) — Graphify's own official integration path, not a
  hand-rolled MCP entry.
- **No `--strict` mode** — default soft nudge coexists better with QMD's
  existing soft CLAUDE.md rule than strict mode's first-Read-blocking behavior.

## Research grounding (already done, don't redo)

Graphify's own `BENCHMARKS.md`: on ERPNext (~1M LOC), graph-expand ties/
slightly loses to dense vector RAG on QA accuracy (76%/0.844 vs 76%/0.848).
Graph's real edge is structural multi-hop questions (call graphs, path
tracing) that QMD's chunk-retrieval literally cannot answer — a query-type
difference, not a codebase-size threshold. Also: the GitHub repo shows
100k+ stars but was created 2026-04-03 (~4 months old at research time) —
treat the star count as likely inflated/bot-driven; the underlying code and
packaging are legitimate and well-built regardless.

## Environment note

Rancher Desktop on this machine has been unstable this session — worth
checking for recurring backend fragility before assuming a config/script bug
if Docker commands fail mysteriously in a future session.

## Also relevant

Earlier this session: `v0.7.2` (QMD docs sync) was committed and pushed to
`origin/main`, then a full cleanup+redeploy of that v0.7.x environment
succeeded (dev-container/dev-mongodb/dev-mongo-express healthy, `set_backend`
cloned clean, QMD per-repo index working). That's what was running when
Rancher Desktop started failing — after reboot, a plain `deploy-dev.ps1`
re-run (no Graphify changes yet) should restore that v0.7.2 baseline before
Step A begins.
